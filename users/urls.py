from django.urls import path
from . import views

urlpatterns = [
    # path("", views.Feeds.as_view()),
    # path("<int:feed_id>", views.Modify_Feed.as_view()),
    # path("<str:username>", views.FeedsByUsername.as_view())
]
